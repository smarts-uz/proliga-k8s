apiVersion: v1
kind: Service
metadata:
  name: postgrest
  namespace: proliga-db
spec:
  selector:
    app: postgrest
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP